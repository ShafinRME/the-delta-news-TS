import React from 'react';

const SliderTitle = ({ title }) => {
    return (
        <>
            <h1 className="text-lg lg:text-2xl font-bold mt-4 text-secondary">{title}</h1>
        </>
    );
};

export default SliderTitle;